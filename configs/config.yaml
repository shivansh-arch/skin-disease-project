model:
  name: efficientnet_b0
  num_classes: 6

training:
  batch_size: 32
  epochs: 10
  lr: 0.001

paths:
  raw_data: data/raw
  processed_data: data/processed
  model_path: models/trained_model.pt